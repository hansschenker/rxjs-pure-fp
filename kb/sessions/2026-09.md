# Session log 2026-09

## 2026-09-01 — M15 boundary & collection operators landed (18 features) <!-- session:0eb435a7-3ce5-4478-9577-825c948d6a47 -->
- Project: rxjs-pure-fp (branch main)
- Landed M15 (Session 4 of the 4-session re-plan): buffer family (`buffer`, `bufferCount`, `bufferTime`, `bufferToggle`, `bufferWhen`), window family (`window`, `windowCount`, `windowTime`, `windowToggle`, `windowWhen`), `groupBy` (with `GroupedObservable`), `partition` (creation + compat operator form), and `count`/`max`/`min`/`every`/`find`/`findIndex`.
- Pinned exact semantics by reading `node_modules/rxjs/dist/esm/internal/operators/*.js` for all 18 features before implementing — the project's standing rule, applied again without deviation.
- Buffer/window families implemented as boundary-policy pairs sharing one materialization machine each; added a repeating variant of `executeSchedule` (`schedule-work.ts`) to back the time-bucketed boundary operators.
- Only real bump: `bufferTimeCreationInterval` differential test passed 3x standalone but flaked once inside the full `npm run verify` gate — root cause was ~15ms margins between scripted pushes and buffer boundaries, and the pureFp vs rxjs adapters run the same scenario at different wall-clock moments so timer drift under parallel-test load could tip a value into a different bucket per side. Fixed by widening margins to 30-100ms (see memory: differential-timer-test-margins). Confirmed 3x clean after the fix.
- Heredoc-via-Bash mangled multi-line file writes for `window-time.ts`/`window-toggle.ts`/`window-when.ts` — fell back to the Write tool for those files instead of debugging the heredoc.
- Root export parity moved 101/175 (57.7%) → 119/175 (68.0%), exactly the planned Session-4 target; 0 unexpected exports.
- Full verify gate green: 141/141 unit, 244/244 differential (42 new M15 traces), architecture gate over 114 source files, dist check over 228 files.
- Regenerated `feature-parity-list.md` from the built dist against `reference/exports.json` via a throwaway Node script (inline `node -e` shell-escaping failed, same as prior sessions — write the script to a temp file instead).
- Updated EXECUTION-PLAN/SEMANTICS/RXJS-7.8.2-PARITY/README in the same commit as the feature code (`b30d61d`), per established discipline; kb sync kept as a separate `chore:` commit (`1d00205`), both pushed to `origin/main`.
- Session closed cleanly per Hans's request ("commit and push, then we do Session 5 in a fresh session"); Session 5/M16 (Creation & Interop, 18 features → target 137/175) is fully scoped in `docs/EXECUTION-PLAN.md` for the next session to pick up cold.
