# My patterns

Durable working patterns, conventions, and preferences — curated by the
SessionEnd summarizer under the rules in `kb/README.md` (merge, don't append;
150-line hard cap).

- Drives large refactors through short, milestone-referencing commands ("land M06", "land F1 and promote the experimental modules", "land M13") and expects Claude to pull scope from the repo's own planning docs (`docs/EXECUTION-PLAN.md`, `docs/FP-ROADMAP.md`, `docs/RXJS-7.8.2-PARITY.md`) rather than having it restated. Keep those docs current after every landed item — they're the real spec.
- Treats "landed" as a certified state, not just code written: a milestone isn't done until it's differentially traced against the real oracle package, the full `npm run verify` gate is green, and the milestone docs (EXECUTION-PLAN/ARCHITECTURE/SEMANTICS/PARITY/README) are updated in the same turn. Confirmed repeatedly without pushback across many milestones this session — follow this discipline by default on this project, don't wait to be asked.
- Deliberately splits large work across sessions at clean, gate-green, pushed commit points ("we do M13 in a fresh session", "Checkpoint Charlie") rather than one continuous session — a fresh session starting from a pushed commit is treated as equally well-informed since the planning docs carry the state. When scoping several future sessions at once, expects exact per-session counts computed against the real remaining-work list (e.g. "4 sessions, 18/18/19/19 features" against the 74 missing exports), not rough/qualitative splits.
- Wants derived artifacts (parity tables, feature counts, status docs) computed from the actual built code against a real oracle/manifest, not hand-typed from memory — caught and corrected a remembered test-count figure mid-session; re-derive from source when producing any summary table or number.
- For RxJS-parity work, expects semantics pinned by reading the installed `node_modules/rxjs` source directly before implementing, not from memory of RxJS behavior — standard step in every milestone, not a one-off.
- When Claude flags low context mid-task and offers to pause, Hans's default answer is "keep going" / "continue" (once explicitly: "it is very precious work") — bias toward finishing the in-flight milestone rather than stopping early unless context is truly exhausted.
- Wants `commit`/`push` only on explicit request, exactly per CLAUDE.md, and often bundles it with a session-boundary signal ("commit and push, then we do M13 in a fresh session") — treat that phrasing as both a commit instruction and a cue that the next prompt starts a new logical session.
